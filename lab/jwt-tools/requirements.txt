# Used by generate_jwt.py to sign tokens with the local RSA private key.
authlib>=1.3.0

# Used by pem_to_jwk.py to convert public_key.pem -> public_key.jwk(s).
# authlib pulls cryptography in transitively, but pin it explicitly so the
# JWK conversion doesn't break if authlib changes its dependency tree.
cryptography>=42.0.0
